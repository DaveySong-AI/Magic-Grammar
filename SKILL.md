---
name: kids-html-course-creator
description: 专门为儿童（小学/初中）制作互动HTML课件的技能。当用户提供课程大纲、讲解脚本/解说词、随堂测试，要求生成适合孩子的HTML课件、课程视频脚本、带语音讲解的互动课件时使用。输出包含魔法主题视觉、AI生成插图、女老师语音讲解、互动测试、自动播放翻页、全部答对后庆祝结束页的单文件HTML。页面数量和内容完全根据用户提供的课程大纲和提示词灵活决定，不做固定限制。触发关键词：儿童课件、小学课件、初中课件、HTML课件、互动课件、课程视频、带语音课件、给孩子做课件。
---

# 儿童HTML课件制作器

将课程大纲、讲解脚本、随堂测试转化为适合小学/初中生的魔法主题互动HTML课件，含AI插图、女老师语音、互动测试、自动播放。

> **核心原则**：页面数量、插图数量、测试题数完全根据用户提供的课程大纲和提示词灵活决定，本Skill中的所有数字和页面结构仅作参考，不作为硬性限制。内容多就多做几页，内容少就精简，以用户需求为准。

## 输入要求

用户需提供（至少提供前两项）：
1. **课程大纲/讲解脚本**：Markdown或文本，包含逐页内容、标题、核心知识点、例句
2. **随堂测试**（可选）：选择题/判断题/配对题，含答案和解析
3. **课程基本信息**：课程名称、课次、总课次、目标受众

若用户未提供完整信息，优先按合理默认推进，不为小问题反复发问。

## 6步工作流

### Step 1：读取并解析输入
- 完整读取所有附件，提取：课程标题、课次信息、逐页内容、讲解词、测试题目及答案
- **根据课程大纲的内容量灵活规划页面总数**：每个核心知识点至少1页，内容丰富的知识点可拆成2-3页，内容少的可合并。不设上下限，以讲清楚内容为目标
- 推荐页面类型（按需选用，非强制）：封面、课程地图/导入、内容页（N页，根据知识点数量）、易混辨析（如有）、总结+练习、互动测试（如有测试题）、庆祝结束页（如有互动测试）
- 为每页提炼：页面标题、核心内容、讲解词（100-300字，儿童化语言）

### Step 2：生成关键插图（image_gen）
根据课件内容和页面规划，按需生成插图，卡通风格、适合儿童、无文字：
- **封面图**（16:9）：课程主题相关的可爱角色/魔法场景，深蓝紫星空背景
- **内容插图**（按需）：重要知识点的可视化插图、场景图、示意图
- **总结/课程地图图**（16:9，如有需要）：与课程结构相关的可视化插图
- **庆祝结束页图**（16:9，如有庆祝页）：孩子们庆祝完成课程，有奖杯、烟花、彩虹、城堡

插图数量根据内容需要灵活决定，不固定。生成后记录每张图的CDN URL。

### Step 3：生成女老师讲解音频（text_to_audio）
为**每一页**（含封面、测试页、庆祝页等所有页面）生成一条女老师讲解音频：
- Prompt统一格式：`一位温柔亲切的30岁女老师，正在给小学生在线讲课。声音甜美有亲和力，语速适中，语调有起伏有感情。+ 该页讲解词`
- 并行批量生成（每次最多5条，分多批完成）
- 音频数量 = 实际页面数量，根据Step 1的规划决定
- 记录每条音频的CDN URL，按页码顺序存入数组

### Step 4：基于模板生成HTML
读取 `assets/course-template.html` 模板，根据实际页面规划替换占位符和内容：
- 基础信息占位符：`{{COURSE_TITLE}}`、`{{LESSON_TITLE}}`、`{{LESSON_INFO}}`等
- 插图URL：`{{COVER_IMAGE}}`及其他内容插图
- `{{AUDIO_URLS}}`：音频URL数组（JS数组格式，按页码顺序，数量=实际页数）
- `{{SLIDES_HTML}}`：所有内容页的HTML（按实际页面规划生成，页数灵活）
- `{{QUIZ_QUESTIONS}}`：互动测试题HTML（题数根据用户提供的测试题决定）
- 庆祝页：如有互动测试则保留，否则可省略

页面HTML结构规范见 `references/page-structure.md`（仅作参考，可根据内容灵活调整）。

### Step 5：验证课件（含Bug验证环节）
1. 用浏览器打开HTML，基础功能检查：
   - 所有页面正常显示，插图加载成功
   - 翻页功能正常（按钮、键盘、页码点）
   - 音频播放正常（点击播放、暂停、停止），每页都有对应音频
   - 自动播放模式：开启后自动播放+翻页
   - 互动测试（如有）：答题有反馈，全部答对后出现庆祝按钮
   - 庆祝页面（如有）：按钮跳转正常，显示证书和祝贺
2. 检查文件为单文件HTML，所有资源通过CDN加载

#### Bug验证环节（必须逐项检查，避免踩坑）
**A. 内容一致性检查**
- [ ] 逐页核对每课标题、学习目标、核心知识点，**禁止多课内容重复**（批量生成时最易犯：第2课起复制第1课内容未替换）
- [ ] 音频URL数组长度 === 实际slide数量，且顺序一一对应（第N页音频对应第N条URL）
- [ ] 音频数组中**禁止含`//`注释**（合并单文件时注释会导致后续URL被当作注释截断）
- [ ] 所有变量声明无重复（如starsContainer不能声明两次），需要重新赋值的变量用`let`不用`const`

**B. HTML结构检查**
- [ ] 每个`<div class="slide">`必须正确闭合，**禁止多余的`</div>`**（会导致后续slide被排除在容器外，`querySelectorAll('.slide')`返回数量不足）
- [ ] 多课合并单文件时，**禁止重复ID**（如`id="miniMap"`每课都有会导致`getElementById`只返回第1课的）；改用class或通过当前课程容器查找
- [ ] 内容多的页面（如第22课）：`.scroll-area`必须设置`min-height:0; height:0; flex:1`，否则内容溢出无法滚动

**C. 自动翻页逻辑检查**
- [ ] 互动测试页（倒数第2页）：语音播完后**不自动翻页**，等待学生手动做题翻页（条件：`currentSlide === totalSlides - 2`时跳过自动翻页）
- [ ] 最后一页（庆祝页）：手动翻到后**自动播放语音**
- [ ] 其他内容页：语音播完延迟800ms后自动翻页
- [ ] 自动翻页条件：`ttsAutoPlay && currentSlide < totalSlides - 1 && !isQuizPage`

**D. 移动端适配检查**
- [ ] 顶部TTS面板：移动端强制`flex-wrap:nowrap`一行显示，按钮文字缩小到10px以下，去掉"语速"文字标签只保留下拉框
- [ ] slide顶部padding：移动端≥9vh（小屏10vh），确保标题不被顶部控件遮挡
- [ ] 翻页按钮：移动端缩小到36px（小屏30px），避免遮挡内容
- [ ] 底部状态栏：移动端上移缩小，不遮挡内容
- [ ] 句子结构图/对比表格：移动端`flex-wrap:nowrap`或缩小尺寸，避免元素掉行

**E. 排版与响应式检查**
- [ ] 绝对定位元素（如盒子上方的标签）：**禁止用`position:absolute`叠加在内容上方**，容易被overflow截断；改为正常流的独立元素
- [ ] 为移动端做的缩小调整**必须写在`@media (max-width:768px)`内**，PC端默认用大尺寸充分利用宽度（禁止用内联固定小尺寸影响PC端）
- [ ] 文字换行：长英文单词/句子设置`word-break:break-word`，避免溢出
- [ ] 每页实际渲染后检查：无文字被截断、无元素重叠、无空白区域过大

**F. 单文件合并专项检查**
- [ ] 合并后在浏览器控制台确认无JS报错
- [ ] `querySelectorAll('.slide').length` === 预期页数
- [ ] 每课切换后迷你地图、进度条等元素正确更新（不残留上一课内容）
- [ ] localStorage的key唯一（如`englishGrammarProgress`、`englishGrammarWrongBook`），不与其他应用冲突

**G. 内容审校回归检查（改动 data-note / 可见卡片后必查）**
- [ ] `python3 check_quotes.py index.html` 通过（**data-note 值内禁止英文直引号**，否则属性截断、解说词乱码）
- [ ] 改动过的页：**解说词与可见卡片口径一致**（不能一边说"that可省"一边卡片写"that不能省"）
- [ ] 批量修改后**逐处回读**完整句子：无改过头（语义与原书相反）、无残留旧句、无衍字
- [ ] 登记页码以 `<!-- P8 -->` 注释标签为准（用 `check_page_refs.py` 核对），不用 data-note 序号推断
- [ ] 自编例句补卡后打「补充举例（原书未提）」标签；仅改解说词时才登记 TTS 重录

### Step 6：交付
用 `present_files` 交付HTML文件，说明：
- 课件实际页数、包含功能
- 使用方法（双击打开、开启自动播、互动测试）
- 浏览器建议（Chrome/Edge/Safari）

## 页面设计原则（参考，非强制）

### 推荐页面类型（按需选用）
- **封面页**：课程标题、副标题、课次徽章、封面插图
- **课程地图/导入页**：全书/全系列总览、本课位置、学习目标（如有系列课程）
- **内容页**（核心，数量灵活）：每页一个核心知识点，含标题、可视化图示、例句卡片、小提示
- **易混辨析页**（如有易混概念）：对比表格/左右分栏
- **总结+练习页**：知识点总表、课后练习题、下节课预告
- **互动测试页**（如有测试题）：题目数量根据用户提供的测试题决定
- **庆祝结束页**（如有互动测试）：庆祝插图、祝贺标题、课程完成证书、下节课预告

### 内容页设计原则
- **大字号**：标题clamp(28px,4vw,52px)，正文clamp(14px,1.8vw,22px)
- **可视化优先**：用彩色盒子、图标、对比表格展示结构，避免大段文字
- **例句卡片**：英文高亮关键词，中文翻译小字，左侧彩色边框
- **儿童化语言**：讲解词用比喻、提问、感叹，避免学术术语
- **魔法主题**：深蓝紫背景+金色高亮+青色/粉色辅助色，星空动画

## 互动测试规范（参考，题数灵活）
- 题目数量根据用户提供的测试题决定，不固定
- 题型支持：选择题（4个选项）、判断题、配对题
- 点击选项后：正确显示绿色反馈+解析，错误显示红色反馈+正确答案
- 每道题只能答一次（答完禁用所有选项）
- 跟踪答对题数，全部答对后0.8秒显示金色脉冲按钮："🎉 全部答对！点这里查看庆祝页面 →"
- 点击按钮跳转到庆祝页（如有）

## 自动播放规范
- 右上角控制面板：播放/暂停/停止按钮、自动播开关、语速选择（慢/中/快/很快）
- 开启自动播后：立即播放当前页 → 播完等0.8秒 → 自动翻页 → 等0.5秒 → 播放下一页 → 循环到最后一页
- 人为暂停后（ttsIsPaused=true），翻页不会自动播放
- 底部显示播放状态："🎙️ 女老师正在讲解第 X/Y 页" + 声波动画

## 质量检查清单
交付前逐项确认：
- [ ] 所有页面内容完整，无空白页，页数与课程大纲匹配
- [ ] **多课内容无重复**（逐课核对标题、学习目标、知识点，禁止第2课起复制第1课未替换）
- [ ] AI插图均已插入，CDN链接有效（插图数量根据内容需要）
- [ ] 每页都有对应的女老师音频，音频数量=页面数量，顺序一一对应
- [ ] **音频数组无注释、无截断**（合并单文件时`//`注释会导致后续URL失效）
- [ ] 翻页三种方式（按钮、键盘←→、页码点）均正常
- [ ] 音频播放/暂停/停止/语速调节正常
- [ ] 自动播放模式：播放→翻页→播放循环正常
- [ ] **测试页不自动翻页**（语音播完等待学生手动做题），最后一页自动播放
- [ ] 互动测试（如有）：答题反馈正确，全对后出现庆祝按钮
- [ ] 庆祝页（如有）：按钮跳转正常，证书内容正确
- [ ] 讲解词开关正常显示/隐藏
- [ ] **内容多的页面可滚动**（.scroll-area设置min-height:0;height:0）
- [ ] **无多余`</div>`**（每个slide正确闭合，querySelectorAll返回数量正确）
- [ ] **无重复ID**（多课合并不用getElementById找重复ID，改用class+当前容器）
- [ ] **移动端适配**：顶部一行显示不换行、标题不被遮挡、翻页按钮不遮挡内容、句子图不换行
- [ ] **响应式正确**：PC端大尺寸充分利用宽度，移动端缩小样式写在@media内
- [ ] **无绝对定位元素被截断**（盒子上方标签改用正常流独立元素）
- [ ] 文件为单HTML，无外部依赖（除CDN图片/音频）
- [ ] 在Chrome/Edge中打开无报错，控制台无JS错误

## 常见Bug与避坑指南（实战总结）

### 1. 多课内容重复（第2课起学习目标一样）
- **现象**：第1课正常，第2-22课的学习目标、部分内容和第1课完全相同
- **原因**：批量生成课程时，循环中复制了第1课的HTML模板但未替换课程特定内容
- **避坑**：生成后逐课抽查第2课、中间课、最后一课的标题和学习目标，确认内容不同；用脚本批量替换时确认占位符全部替换

### 2. 音频数组被注释截断
- **现象**：部分页面没有语音，控制台报音频URL undefined
- **原因**：`allAudioUrls = [...]` 数组中某行末尾有 `// 注释`，合并单文件时换行被压缩，导致 `]` 跟在 `//` 后被当作注释，后续URL全部失效
- **避坑**：音频数组中**绝对禁止**写 `//` 注释；提取URL时用脚本去掉所有注释

### 3. 多余`</div>`导致slide数量不足
- **现象**：合并单文件后，每课只有前2页能翻，后面页面空白；`querySelectorAll('.slide')`只返回2个
- **原因**：某课第2页（课程地图）的HTML中有一个多余的`</div>`，提前闭合了course-presentation容器，后续slide不在容器内
- **避坑**：合并前用HTML验证工具检查每个课程文件的div嵌套；合并后验证`slides.length === 预期页数`

### 4. 重复ID导致元素不更新
- **现象**：第1课迷你地图正常，第2课以后迷你地图区域空白
- **原因**：每课都有`id="miniMap"`，`getElementById`永远只返回文档中第一个（第1课的），切换课程后找不到当前课的迷你地图
- **避坑**：多课合并不用固定ID，改用`class="mini-map"`，通过`currentCourse`容器`.querySelector('.mini-map')`查找

### 5. 内容多无法滚动
- **现象**：第22课第6页内容多，右侧有滚动条但拖不动，底部文字被截断看不到
- **原因**：`.scroll-area`在flex容器中没有设置`min-height:0`，flex子项默认不收缩，内容溢出但不可滚动
- **避坑**：所有可滚动区域必须设置`min-height:0; height:0; flex:1; overflow-y:auto`

### 6. 测试页自动翻页学生来不及做题
- **现象**：互动小测试页语音播完自动跳到庆祝页，题还没做
- **原因**：自动翻页逻辑没有排除测试页，所有页面语音播完都自动翻
- **避坑**：自动翻页条件增加`!isQuizPage`判断，测试页（`currentSlide === totalSlides - 2`）不自动翻页；最后一页（庆祝页）保持自动播放

### 7. 移动端顶部拥挤换行
- **现象**：手机微信打开，顶部TTS面板换行成两行，遮挡标题
- **原因**：顶部控件（首页按钮+讲解词+播放控制+语速+自动播）总宽度超过手机屏宽，flex-wrap默认wrap
- **避坑**：移动端媒体查询中设置`.tts-panel { flex-wrap:nowrap; }`，按钮文字缩小到10px，去掉"语速"文字标签只保留下拉框，gap缩到3px

### 8. 绝对定位元素被截断
- **现象**："像一座桥"标签显示不全，只露出一半
- **原因**：标签用`position:absolute; top:-30px`定位在盒子上方，父容器`overflow:hidden`导致超出部分被截断
- **避坑**：盒子上方的说明标签**不要用绝对定位**，改为正常流的独立块级元素（如`<div class="bridge-label">`），居中显示

### 9. 移动端缩小影响PC端
- **现象**：为手机适配缩小了图文尺寸，结果PC浏览器里也变小了，和其他页面不协调
- **原因**：用了内联样式`style="padding:8px 12px;font-size:13px"`，在所有屏幕尺寸都生效
- **避坑**：PC端默认用大尺寸（充分利用宽度），移动端缩小样式**必须写在`@media (max-width:768px)`内**；禁止用内联固定小尺寸

### 10. const变量无法重新赋值
- **现象**：切换课程时报错`Assignment to constant variable`
- **原因**：`const slides = []`声明后，`showCourse`中尝试`slides = ...`重新赋值
- **避坑**：需要在函数中重新赋值的变量用`let`声明

### 11. 庆祝页底部内容被截断
- **现象**：每课最后一页（庆祝页）底部的"下节课预告"和"第X/22课已完成"徽章显示不全，被挤出可视区域
- **原因**：庆祝页（slide cover）内容多（大图+标题+证书+预告+徽章），但没有用`.scroll-area`包裹，内容溢出不可滚动
- **避坑**：所有页面（包括封面页、庆祝页）只要内容可能超过一屏，都必须用`.scroll-area`包裹；庆祝页封面图建议控制在42vw以内，避免图片过大挤占空间

## 内容审校与回归避坑指南（实战总结·审校期）

> 课件上线后的内容审校（对照原书/脚本逐页核对）会遇到与开发期不同的一类坑。以下经验来自 Magic Grammar 审校 issue #28~#32 的回归修复，**改动 `data-note`（解说词）或可见卡片前必读**。

### 12. data-note 属性值内禁止英文直引号（最易爆）
- **现象**：某页解说词显示到一半断掉，引号后的内容变成页面上乱码文本；对应音频讲的是完整版，讲解词面板却是截断版（issue #28：8 处）
- **原因**：`data-note="..."` 用英文双引号包裹属性值，值内的英文 `"` 会让 HTML 解析器**提前闭合属性**，后续内容溢出为标签外文本
- **避坑**：
  - 所有解说词里的引用/对话，**一律用中文引号「」**，禁止在 data-note 值内出现英文 `"`
  - 批量改讲解词时，凡是含引号的内容都要检查
  - **提交前必跑**：`python3 check_quotes.py index.html`（扫描全部 data-note 值内是否残留英文双引号，当前 351 个 data-note 全通过为绿灯）

### 13. 解说词改了，可见卡片必须同步（同一页不能有两个"真相"）
- **现象**：页面可见卡片写着"that 不能省"，但解说词已按原书改成"that 可省（视意思清楚）"——学生听到和看到的矛盾（issue #29：5 处）
- **原因**：审校只改了 `data-note`（朗读/讲解词），忘了同步该页可见的 PPT 卡片文本；或只改了卡片、解说词没跟上
- **避坑**：
  - 任何一处内容改动，**同时检查该页两处呈现**：① `data-note`（解说词，被朗读）② 可见卡片/正文（被阅读）
  - 改术语、例句、规则时，两边措辞必须口径一致
  - **提交前跑**：`python3 check_slide_sync.py index.html`（提示工具，列出"解说词有例句、卡片未必有"的页供人工核对；凡已补卡并打「补充举例（原书未提）」标签的自动放行）

### 14. 批量精修会改过头 / 残留旧句 / 衍字（改后必须回读）
- **现象**：
  - 改过头：卡片"语法可成立、意思稍异"被改成"词类/意思都不对"（与原书相反，issue #30）
  - 残留：删了自编例句但卡片里旧句没删干净
  - 衍字：把"will、can"误写成"助动词 why"（issue #30）
- **原因**：批量替换脚本/大段重写时未逐句核对语义，或手滑多打/漏删
- **避坑**：
  - 批量修改后**必须逐处回读**改动的完整句子，确认语义与原书/脚本一致、无衍字无漏字
  - 敏感术语（省略、可省、必须、不能）尤其要逐字核对，方向错了意思就反了
  - 改完对照原书原文或课程大纲脚本再读一遍

### 15. 登记页码以 slide 注释标签为准，不要用序号推断
- **现象**：CHANGELOG/TTS 清单里登记的"第22课P7"实际是"第22课P8"（issue #31：8 处全部 -1）
- **原因**：用"该课内第 N 个 data-note 序号"推断页码，但有的课第 1 页（封面）无 data-note，导致序号比实际页码少 1
- **避坑**：
  - 页码一律以源码里的 `<!-- ===== P8 标题 ===== -->` **注释标签**为准，不要数 data-note 个数
  - 登记位置时用该页 data-note 里的**独特锚点文字**（而非通用词），方便日后检索
  - **提交前跑**：`python3 check_page_refs.py index.html`（给定锚点关键词输出其所在 slide 的注释页码，核对登记是否错位）

### 16. 自编例句若与原书兼容，先补卡打标签，不要改解说词
- **现象**：解说词里有自编例（原书没有），但卡片没展示；学生听到的例句看不到（issue #32：9 页）
- **原因**：讲解时即兴补充的例句没同步进可见卡片
- **避坑**（按"卡片吸收为主"原则）：
  - 自编例句若与原书内容兼容且有教学价值 → **补进该页卡片**，并加小标签「补充举例（原书未提）」
  - **解说词不动、音频不重录**（补卡不涉及音频）
  - 仅在解说词与卡片**冲突 / 含错误规则**时，才改解说词（此时该页需登记 TTS 重录）
  - 改完在《TTS待重录清单.md》登记：**改解说词的页才登记**，纯补卡的页不登记

### 17. 改完解说词记得同步维护锚点与登记
- **现象**：`check_page_refs.py` 里的锚点文字指向已被删除的旧句（如第22课P8 的"I'm tired,"said John），工具找不到锚点报"找不到锚点"
- **原因**：解说词内容被重写后，登记过的锚点文字失效
- **避坑**：重写某页解说词后，同步更新 `check_page_refs.py` 中该页锚点为**新解说词里的现存文字**，并在 TTS 待重录清单更新对应说明

### 18. 工具脚本是防复发防线，提交前必跑
- 本项目沉淀了 3 个校验脚本，**每次改动 index.html 后提交前都跑一遍**：
  - `python3 check_quotes.py index.html` — data-note 引号截断（硬校验，失败必须修）
  - `python3 check_page_refs.py index.html` — 登记页码与注释标签一致（核对用）
  - `python3 check_slide_sync.py index.html` — 解说词-卡片脱节提示（辅助，不阻断，供人工核对）
- 这三道防线能挡住 #28/#31/#32 三类最易犯的回归
